# 测试驱动开发：本分支的开发纲领

## 为什么是测试驱动

我们在做一件特殊的事：为神经网络写代码生成器。

我们不是神经网络的专家。我们不知道市场上会出现什么样的 ONNX 模型——什么样的算子组合、什么样的量化参数、什么样的拓扑结构、什么样的边界尺寸。我们面对的是一个输入空间无限大、且不断膨胀的系统。

传统开发方式在这里行不通。你不能"先想清楚所有情况再写代码"，因为你不可能想清楚。你也不能"先把代码写完再补测试"，因为你根本不知道什么叫写完。

所以我们选择测试驱动开发。

## 我们的 TDD 不是教科书里的 TDD

教科书里的 TDD 是：写一个单元测试，让它失败，然后写代码让它通过。那是在你已经知道需求的前提下。

我们的情况不同。我们的需求来自于真实世界的 ONNX 模型，来自于我们还没见过的网络结构。我们的 TDD 是：

1. **构造一个我们认为应该能处理的 ONNX 模型**（测试用例）
2. **准备输入数据集和参考输出**（数值验收资产）
3. **把它喂给我们的代码生成器**（结构执行）
4. **运行生成的 C 推理代码并对比 ONNX Runtime**（数值执行）
5. **观察生成器是通过了、拒绝了、崩溃了，还是数值不一致**（评估）
6. **如果行为不符合预期——修复代码生成器**（迭代）
7. **如果通过了——这个模型形态永久纳入回归集**（沉淀）

每一个通过的测试用例，就是我们向市场多承诺了一种能力。每一个被正确拒绝的测试用例，就是我们对自己边界的一次精确划定。

## 核心循环：测试即需求

```
              ┌────────────────────────────┐
              │  构造新的 ONNX 测试模型      │
              │  （定义"什么是对的"）        │
              └─────────────┬──────────────┘
                            ▼
              ┌────────────────────────────┐
              │  执行 converter → codegen   │
              │  → compile → run           │
              └─────────────┬──────────────┘
                            ▼
                   ┌────────┴────────┐
                   │  结果符合预期？  │
                   └────────┬────────┘
                    ╱                ╲
                 是                    否
                  │                    │
    ┌─────────────▼───────┐   ┌───────▼─────────────────┐
    │  纳入回归测试集      │   │  定位缺陷，修复代码      │
    │  能力边界向外扩展一步 │   │  生成器，重新执行        │
    └─────────────────────┘   └─────────────────────────┘
```

这个循环永不停止。我们的代码生成器的能力边界，等于我们的测试集覆盖的边界。

## 测试用例的三重角色

在这个项目中，测试用例同时扮演三个角色：

- **需求规格**：每个测试用例精确定义了"代码生成器应该对这种输入产出什么结果"。没有模糊的自然语言需求文档，测试用例本身就是最精确的需求。
- **能力证明**：通过的测试集，就是我们能对外宣称"支持"的模型形态集合。白名单不是人写的——白名单是测试跑出来的。
- **回归防线**：每一次修改代码生成器，所有历史测试用例必须继续通过。新功能不能打破旧功能。

## 能力集：测试即产品边界

**能力集**是本项目最核心的资产概念。

我们的代码生成器能做什么、不能做什么，不是由文档宣称的——而是由测试通过与否决定的。通过的测试用例集合，就是我们产品的"能力集"。

```
                    ┌────────────────────────────────────────┐
                    │           能力集 (Capability Set)       │
                    │                                        │
                    │   每个通过的测试用例 = 一项已验证能力    │
                    │   能力集 = 所有 PASS 用例的集合         │
                    │   能力集边界 = 产品的能力边界            │
                    └────────────────────────────────────────┘
```

### 能力集的生命周期

1. **新增能力**：编写新测试用例 → 执行失败 → 修复代码 → 执行通过 → 能力集扩展
2. **保护能力**：每次代码修改后全量回归 → 所有已有能力不退化
3. **度量产品**：能力集大小 = 产品成熟度；能力集增长速度 = 开发效率

### 为什么能力集如此重要

我们不是神经网络专家。我们不知道用户会用什么模型，但我们需要对用户做出承诺——"我们的代码生成器支持 X 类模型"。这个承诺的唯一可信基础就是：**我们测过了，它通过了**。

每次迭代的开发目标本质上只有一个：**让能力集变大**。新功能开发是扩展能力集，bug 修复是修复能力退化，性能优化是在不缩小能力集的前提下改进。

### 能力集的记录与追踪

能力集状态持久化在 `tdd/CAPABILITIES.md` 中。该文件由测试脚本自动生成，它是产品当前能力的权威声明。Git 跟踪该文件——每次能力集变化都反映在提交历史中，你可以追溯"什么时候获得了这个能力"。

**CAPABILITIES.md 是只读输出，不是手写文档。** 它的内容完全由测试结果决定。人不能声称能力——只有通过测试才能声称能力。

### target 与 baseline

TDD 执行分为两种模式：

- `target`：执行全部目标用例，生成 `CAPABILITIES.md`，失败项就是下一轮开发任务单。
- `baseline`：只执行已经纳入稳定回归门禁的用例，用于确认已承诺能力没有退化。

`target` 可以失败，因为它承载未来能力；`baseline` 不应失败，因为它保护当前已经确认的能力。只有全量 `target` 执行才会重写 `CAPABILITIES.md`，避免单个用例或分类测试把能力集误缩小。

### PASS 的含义

对 `ok` 用例，PASS 不只是 codegen 报告为 `ok`。测试执行器还会检查：

- 生成的 `model.c` 包含用例要求的 CMSIS-NN API。
- 生成 C 工程可以通过 C99 smoke compile。
- smoke binary 可以启动且不崩溃、不超时。默认 host smoke 会关闭真实 CMSIS-NN 调用，因此返回 `NANOC_STATUS_BLOCKED` 也属于正常保护行为。

这只代表**结构验收 PASS**。它证明生成物存在、可编译、可启动，但不证明推理数值正确。
对完整网络和数值敏感用例，还必须执行数值验收：

- 使用同一组输入样本运行原始 ONNX。
- 编译并运行生成 C 工程，启用真实 CMSIS-NN。
- 将 C 输出反量化到 ONNX 输出语义，比较 top1、误差阈值和饱和情况。
- 若 ONNX 与 C 输出差异超过用例阈值，即使结构验收 PASS，也不能宣称该完整网络转换正确。

对 `blocked` / `unsupported` 用例，PASS 表示 pipeline 明确拒绝并返回非零，而不是崩溃或静默产出可交付代码。
对 `oversize` 用例，PASS 表示生成语义本身没有进入 blocked/unsupported，但给定目标内核、SRAM/Flash 预算无法承载该模型。

### 生成物优先原则

报告不是最终事实源，生成物才是最终事实源。任何一次 `ok` 判定都必须以
`cmsis-codegen/src/model.c` 和实际编译结果为准，而不能只看 `codegen_report.txt`
或 `pipeline_report.md` 中的状态字段。

一次 `ok` 生成物至少必须满足：

- `nanoc_model_status()` 返回 `ok`，且 `nanoc_model_run()` 中存在真实运行路径。
- `nanoc_model_run()` 内部必须包含该模型运行期节点对应的 CMSIS-NN 调用，不能只有
  `Generated execution trace` 注释或 fallback stub。
- C 预处理结构必须完整，不能出现孤立的 `#else`、`#endif`、缺失 `#if` 等语法结构错误。
- C99 smoke compile 必须通过；未执行 compile 时，只能称为“分析通过”或“生成候选”，
  不能称为“正确生成”。
- 对多分支网络，必须检查生成代码是否按 tensor name 读取历史中间张量；不能把
  线性 `current_input/current_output` 误认为残差或 concat 已正确实现。

如果报告显示 `ok`，但 `model.c` 为空运行路径、stub、语法错误、缺少必要 CMSIS-NN
调用或无法编译，这属于**严重假阳性**。严重假阳性优先级高于普通 blocked/unsupported：
必须先把它沉淀为测试用例或验证规则，再继续扩展算子能力。严禁把报告状态直接等同于
产品能力。

### 能力集演进的具体场景

下面这个例子说明能力集在实际产品维护中如何运转：

> **背景**：当前能力集稳定，10 条测试全部通过，产品发布。
>
> **触发**：一个用户反馈某个 ONNX 模型转换结果不符合预期，附上了问题模型。
>
> **第一步 — 不要直接改代码**。拿到问题模型，先分析它涉及哪些能力缺口，把问题分解成若干个测试用例（本例中分解出 5 个），写进 `cases/`，注册进脚本，执行——预期全部失败。这 5 个失败的用例，就是这次修复任务的精确需求规格。
>
> **第二步 — 按测试报告修代码**。迭代修复，逐步让 5 个新用例从 FAIL 变成 PASS。
>
> **第三步 — 全量回归**。5 个新用例全通后，立刻跑全部 15 条。发现原来第 3 条用例突然失败了——说明修复过程引入了兼容性问题，破坏了已有能力。这个问题如果没有全量回归，根本无从发现。
>
> **第四步 — 修复退化，再次全量回归**。直到 15 条全部通过。
>
> **结果**：能力集从 10 扩展到 15，CAPABILITIES.md 自动更新，git 提交记录这次扩展。产品交付的承诺从"支持 10 种模型形态"变成了"支持 15 种模型形态"——每一条都有测试背书。

这个流程有几个关键约束需要严格遵守：

- **用户反馈 → 测试用例，不是 → 直接改代码**。跳过用例直接修复，等于放弃了对这次修复的永久验证手段。
- **新用例 PASS 之前，不允许宣称修复完成**。用例通过才是修复的唯一证明。
- **全量回归是每次交付的强制门槛**，不是可选步骤。新能力不能以损坏旧能力为代价。
- **15 条全通 = 新版能力集诞生**。这是产品向前移动的最小完整单元。

## "预期失败"也是有效的测试

我们的测试用例分两类：

- **预期通过**（`ok`）：代码生成器必须正确生成可编译、可运行的 C 代码。
- **预期拒绝**（`blocked` / `unsupported`）：代码生成器必须明确报错，而不是静默产出错误代码或崩溃。
- **平台超限**（`oversize`）：代码生成器完成语义检查后，由目标平台预算门禁明确拦截。

第二类同样重要。一个代码生成器如果不能正确拒绝它不支持的输入，那它生成的代码就不可信。"知道自己不能做什么"和"知道自己能做什么"一样关键。

## 测试驱动架构演进

当我们写下一个我们明知当前架构处理不了的测试用例时，我们不是在浪费时间——我们是在用测试精确地定义下一轮架构升级的需求。

一个标记为 `blocked` 的测试用例，就是一份写给未来自己的需求规格书。当某天我们决定支持残差连接、支持多分支拓扑、支持动态 Shape，我们不需要重新思考"怎么验证"——测试用例已经在那里等着了。

## 迭代的起点：从测试用例出发

每一轮开发迭代的起点，不是"我觉得应该加什么功能"，而是"测试用例告诉我还缺什么"。

**需求的两个来源**：

1. **人类用户明确提出的需求**——这是外部驱动，直接转化为测试用例再执行。
2. **从现有测试用例集推导出的需求**——这是内部驱动，是日常迭代的主要方式。

第二种方式是本项目的常态。具体做法：

### 每次迭代开始时，必须先审视测试用例

不要直接动手写代码。先做这件事：

1. **遍历所有已存在的测试用例**，了解当前能力边界在哪里
2. **从测试用例出发思考**：有哪些方面没有被考虑到？
   - 当前已通过链路中，是否有参数组合、边界条件、异常路径还没有覆盖？（**细化雕刻**）
   - 是否有新的算子组合、新的拓扑形态、新的量化模式值得探索？（**新场景探索**）
3. **先写测试用例**，定义"什么是对的"
4. **执行测试**，观察失败报告
5. **根据测试报告做开发**——报告告诉你哪里坏了，你就修哪里

### 禁止"凭感觉写代码"

以下行为在本项目中是被禁止的：

- ❌ "我觉得这里可以优化一下" → 直接改代码
- ❌ "这个算子应该也能支持" → 直接写 renderer
- ❌ "这个边界条件可能有问题" → 直接加个 if 判断

正确的做法：

- ✅ "我觉得这里可以优化" → 先构造一个能暴露问题的测试用例 → 跑测试 → 看报告 → 改代码
- ✅ "这个算子应该也能支持" → 先构造该算子的测试模型 → 喂给生成器 → 看它怎么失败 → 按报告修复
- ✅ "这个边界条件可能有问题" → 先构造边界输入的测试用例 → 确认确实有问题 → 再修

**测试报告就是你的开发任务单。** 不需要另外的需求文档、不需要 TODO list、不需要口头讨论——跑一遍测试，失败的那些就是你接下来要做的事。

## 本分支的工作节奏

```
每次迭代 = 审视测试用例 → 发现缺口 → 新增测试 → 执行（预期失败）→ 按报告修代码 → 全量回归 → 下一轮
```

常用命令：

```bash
python tdd/scripts/run_tests.py --validate-only
python tdd/scripts/run_tests.py --mode baseline --generate
python tdd/scripts/run_tests.py --mode target --generate
python tdd/scripts/run_regression.py --generate
python tdd/scripts/run_tests.py --case GEMM_001 --generate
```

具体的测试用例规格见 `tdd/cases/`，当前能力集见 `tdd/CAPABILITIES.md`，迭代记录见 `tdd/iterations/`。本文件不会改变——因为开发理念不会改变。

目录结构、持久 fixture、临时 workdir 和数值测试规则见 `tdd/STRUCTURE.md`。任何人工数据集、真实用户模型或调试样本都必须放入 `tdd/fixtures/`，不得放入 `tdd/work/` 或 `tdd/results/tmp/`。

新增完整网络能力时，优先使用：

```bash
python tdd/scripts/run_tests.py --case TOPO_003 --generate
python tdd/scripts/run_numeric_tests.py --case TOPO_003 --generate
```

## 一句话总结

> 我们不知道世界上有多少种神经网络，但我们测过的，就一定能正确生成。
